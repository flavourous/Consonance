<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Consonance.XamarinFormsView.ChoosePlanView">
	<StackLayout Orientation="Vertical">
		  <ListView HasUnevenRows="true" ItemsSource="{Binding PlanChoices, Mode=OneWay}" SelectedItem="{Binding choicey}">
		    <ListView.ItemTemplate>
	      		<DataTemplate>
		              <ViewCell>
			          		<Label Text="{Binding name}" HorizontalOptions="Fill" VerticalOptions="CenterAndExpand"/>
		              </ViewCell>
        		</DataTemplate>
		    </ListView.ItemTemplate>
		  </ListView>
		  <StackLayout Orientation="Horizontal">
		  	<Button Text="Cancel" Clicked="DoCancel"/>
		  	<Button Text="Ok" Clicked="DoChoose"/>
		  </StackLayout>
	</StackLayout>
</ContentPage>