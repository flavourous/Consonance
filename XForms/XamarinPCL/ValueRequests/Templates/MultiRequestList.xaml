<?xml version="1.0" encoding="UTF-8"?>
<ContentView 
  xmlns="http://xamarin.com/schemas/2014/forms" 
  xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" 
  xmlns:xlib="clr-namespace:XLib;assembly=Consonance.XamarinFormsView.PCL"
  x:Class="Consonance.XamarinFormsView.PCL.MultiRequestList">
  <StackLayout Orientation ="Vertical">    
    <xlib:BindingStackLayout ItemsSource="{Binding itemrequests}">
      <xlib:BindingStackLayout.ItemTemplate>
        <DataTemplate>
          <ContentView Content="{Binding}"/>
        </DataTemplate>
      </xlib:BindingStackLayout.ItemTemplate>
    </xlib:BindingStackLayout>
    <xlib:GridView Headers="{Binding headers}" Items="{Binding items}"/>
  </StackLayout>
</ContentView>
